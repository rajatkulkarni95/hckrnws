// import { AlignCenter } from "styles/";
// import React from "react";
// import MoonIcon from "svgs/moon.svg";
// import SunIcon from "svgs/sun.svg";
// import { Icon } from "../Common/Button";

// type Props = {
//   theme?: string;
//   handleThemeChange: (value: string) => void;
// };

// const ThemeToggler: React.FC<Props> = ({ theme, handleThemeChange }: Props) => {
//   return (
//     <AlignCenter>
//       {theme === "dark" ? (
//         <Icon onClick={() => handleThemeChange("light")}>
//           <SunIcon width={18} height={18} stroke="#FFFFFF" />
//         </Icon>
//       ) : (
//         <Icon onClick={() => handleThemeChange("dark")}>
//           <MoonIcon width={18} height={18} />
//         </Icon>
//       )}
//     </AlignCenter>
//   );
// };

// export default ThemeToggler;
